# Copy this file to .env and fill in values

# URL base prefix for both client (Vite base, Nginx) and server routes
HOME_DIRECTORY=/parse

# Logging (server)
APP_LOG_LEVEL=INFO

# Azure AD (used by both client and server)
# SPA client ID (also used by server as a fallback audience if AZURE_API_AUDIENCE/ALLOWED is not set)
AZURE_CLIENT_ID=
AZURE_TENANT_ID=
# Optional explicit authority override (defaults to https://login.microsoftonline.com/${AZURE_TENANT_ID})
# AZURE_AUTHORITY=
AZURE_CLIENT_SECRET=
AZURE_REDIRECT_URI=https://sense.syniti.com/parse/authorized

# Server-side JWT validation (Flask)
# Optional: space-separated accepted audiences for Bearer tokens.
# If empty, falls back to AZURE_CLIENT_ID.
# AZURE_ALLOWED_AUDIENCE=
# JWKS/OpenID config cache TTL (seconds)
AZURE_JWKS_TTL=300

# Backend (Flask) session auth via MSAL
# Secret for Flask session cookies (required when using backend auth)
FLASK_SECRET_KEY=change-me
